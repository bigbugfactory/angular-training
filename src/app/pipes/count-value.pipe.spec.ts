import { CountValuePipe } from './count-value.pipe';

describe('CountValuePipe', () => {
  it('create an instance', () => {
    const pipe = new CountValuePipe();
    expect(pipe).toBeTruthy();
  });
});
